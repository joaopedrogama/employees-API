name: Deploy pipeline

on:
  push:
    branches:
      - main

jobs:

  build-and-deploy-artifact:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3.4.0
      
    - name: Docker login
      uses: docker/login-action@v2.1.0
      with:
        username: ${{ secrets.DOCKER_USER }}
        password: ${{ secrets.DOCKER_TOKEN }}
        
    - name: Build Docker image
      uses: docker/build-push-action@v4.0.0
      with:
        context: .
        push: true
        tags: |
          joaopedrogama/employees-api:${{ github.sha }}
          joaopedrogama/employees-api:latest
          
  # deploy-EC2:
  #   needs: build-and-deploy-artifact
  #   runs-on: ubuntu-latest
    
    # steps:
    # - name: Deploy using ssh
    #   uses: appleboy/ssh-action@master
    #   with:
    #     host: ${{ secrets.HOST }}
    #     username: ${{ secrets.USERNAME }}
    #     key: ${{ secrets.SSH_PRIVATE_KEY }}
    #     port: 22
    #     script: |
    #       docker stop employees-api
    #       docker rm employees-api
    #       docker pull joaopedrogama/employees-api:latest
    #       docker run -d -p 8383:8080 --name employees-api joaopedrogama/employees-api:latest
